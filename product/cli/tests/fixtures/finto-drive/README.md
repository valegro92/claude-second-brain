# Finto Drive — Corpus sintetico per test Custodia

Questa cartella contiene un mini-dump finto, usato dai test unit/integrazione
di Custodia per verificare il connettore filesystem e l'intera pipeline
`scan → extract → review → write`.

Tutti i nomi (clienti, P.IVA, importi) sono inventati.
